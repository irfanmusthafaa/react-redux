import React from 'react'
import "./Logout.css"

const Logout = () => {
    return (
        <div className="logout">
            <h1> Wellcome <span className="fullname">Irfan</span></h1>
            <button className="logout__btn">Logout</button>
        </div>
    )
}

export default Logout
